import { StylesUtils } from "@utils";

export const styles = StylesUtils.create({
  newsCardIconTextBox: {
    position: "absolute",
    bottom: 3,
    left: 2,
    p: 1,
    px: 2,
    borderRadius: 2,
    backgroundColor: "rgba(0, 0, 0, .5)",
  },
});
