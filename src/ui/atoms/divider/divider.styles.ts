import { StylesUtils } from "@utils";

export const styles = StylesUtils.create({
  vertical: {
    height: "70%",
    borderWidth: 1,
    borderStyle: "solid",
    borderColor: "highlight",
  },
});
