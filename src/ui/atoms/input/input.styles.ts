import { StylesUtils } from "@utils";

export const styles = StylesUtils.create({
  inputBox: {
    borderRadius: 3,
    px: 2,
    color: "inputText",
    backgroundColor: "white",
    boxShadow: "none",
    alignItems: "center",
    borderWidth: 2,
    borderStyle: "solid",
  },
});
